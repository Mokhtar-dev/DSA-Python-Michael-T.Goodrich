"""What parameters should be sent to the Python range() constructor to produce a
range object that generates the sequence: 50, 60, 70, 80?"""

# Answer:
the_sequence = range(50, 90, 10)
print(list(the_sequence))
